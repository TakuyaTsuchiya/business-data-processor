# Blue-Green設定を無効化（シンプルな単一コンテナ構成に変更）
# upstream app_backend {
#     server app-green:8501 max_fails=3 fail_timeout=30s;
#     keepalive 32;
# }

# シンプルな構成（docker-compose.ymlのコンテナ名を使用）
upstream app_backend {
    server business-data-processor:8501 max_fails=3 fail_timeout=30s;
    keepalive 32;
}

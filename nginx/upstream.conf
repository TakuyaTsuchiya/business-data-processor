# Business Data Processor - Upstream Configuration
# This file is dynamically updated during Blue-Green deployment

upstream app_backend {
    # Default: Blue environment
    server app-blue:8501 max_fails=3 fail_timeout=30s;
    
    # Backup server configuration (commented out)
    # server app-green:8501 max_fails=3 fail_timeout=30s;
    
    # Connection settings
    keepalive 32;
}